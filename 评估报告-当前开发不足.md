# Sailing 项目现状评估报告

> 评估依据：
> - `/Users/xz33/Downloads/Sailing (1).md`（First Playable Web Game Brief）
> - 用户提供的 system graph（Sailing - Ian）
> - 当前项目代码（`/Users/xz33/Documents/NYU/Sailing_WorkBuddy`）

---

## 1. 结论速览（一句话）

当前实现是一个**技术导向的 Three.js 航海模拟原型**，核心循环（转舵、调帆、风向、目标浮标）已经跑通；但它**与 brief 要求的“可提交、可展览、面向学习成果”的 first-playable web 游戏还有较大差距**。主要问题集中在：**技术栈与 brief 冲突、项目结构缺失、展览网站缺失、挑战/关卡机制缺失、开发日志缺失**。

---

## 2. 与 brief 的逐项对照

### 2.1 技术栈（严重不符）

| brief 要求 | 当前实现 | 差距 |
|---|---|---|
| Plain JavaScript (ES6) | 使用 ES modules + `import` 语法 | 形式一致，但依赖 Vite 和 Three.js |
| HTML Canvas 2D | 使用 Three.js WebGL 3D 渲染 | 严重偏离 |
| 优先零外部库 | 依赖 `three` 和 `vite` | 严重偏离；需要 npm install 才能运行 |
| 不需要 npm / bundler / framework | 项目依赖 `package.json` + Vite | 严重偏离 |
| 本地静态服务器即可运行 | 需要 Vite dev server | 偏离；离线展览存在风险 |
| 若用外部库需从 CDN 引入并说明 | 没有 CDN 说明 | 缺失 |

**影响：** 这个项目无法直接放入课程 gallery 子目录、用普通文件服务器打开即可运行。高三学生或不熟悉 npm 的展示环境可能无法启动。

---

### 2.2 项目结构（严重缺失）

brief 期望的目录结构：

```text
project/
├── index.html
├── game.html
├── process.html
├── styles.css
├── game.js
├── agent-development-log.md
├── development-brief.md
├── system-graph.png
├── submission-manifest.json
├── preview.png
├── README.md
└── assets/
```

当前实际结构：

```text
Sailing_WorkBuddy/
├── index.html
├── package.json
├── package-lock.json
├── vite.config.js
├── README.md
├── node_modules/
├── dist/
├── docs/
│   └── DESIGN.md
└── src/
    ├── main.js
    ├── scene/...
    ├── physics/...
    ├── boat/...
    ├── controls/...
    ├── ui/...
    └── utils/...
```

**缺失文件：** `game.html`、`process.html`、`styles.css`、`game.js`、`agent-development-log.md`、`development-brief.md`、`system-graph.png`、`submission-manifest.json`、`preview.png`、`assets/`。

**影响：** 项目无法按 brief 进入“提交包装”阶段；展览网站、开发过程展示、提交清单全部空白。

---

### 2.3 集成项目网站（完全缺失）

brief 明确要求网站包含：
- Game Idea（标题、概念、玩家目标、核心学习转变）
- Domain Knowledge（真实领域、新手误解、专家判断）
- System Design（系统图、环境数据、玩家控制数据、计算结果、反馈、成功/失败、挑战预设）
- Development Process（基于 `agent-development-log.md` 的时间线）
- Play the Game（清晰导航到可玩游戏）

当前只有 `index.html` 一个单页面，且内容是游戏 HUD + 画布，没有上述任何网站内容。

**影响：** 项目无法被未参与开发的同学理解；也无法展示“来自生活的游戏设计”方法论和核心学习转变。

---

### 2.4 游戏机制与 system graph（部分偏离）

#### 2.4.1 操作映射

system graph 中定义的玩家输入：
- `A` / `D`：boat direction（转舵）
- `W` / `S`：sail angle change（调帆）
- choose path（选择路径）
- turn timing（转向时机）

当前实现：
- `A`/`D`：转舵 ✅
- `W`/`S`：调帆 ✅
- `Q`/`E`：缩帆/展帆（额外增加，不在 brief 中）
- Space：舵回正
- 没有明确的“选择路径”机制（只有随机目标点）

**影响：** 操作范围比 brief 宽，但核心学习机制（路径选择、之字形抢风）没有显性化。

#### 2.4.2 关卡 / 挑战预设（完全缺失）

system graph 右侧定义了三个关卡：

| 关卡 | 目标 | 风 | 水流 | 岩石 |
|---|---|---|---|---|
| Level 1 | 到达终点 | crosswind | 无 | 无 |
| Level 2 | 限时到达终点 | headwind | 无 | 无 |
| Level 3 | 限时到达终点 | crosswind | 无 | 路径上有 >5 个岩石 |

当前实现：
- 没有关卡概念；
- 目标浮标是随机生成的（`target.spawn()` 在船周围 150~300m 随机位置）；
- 没有计时器；
- 没有失败条件；
- 没有岩石/障碍物；
- 没有“起点→终点”的固定航线。

**影响：** 没有“挑战预设”，也就无法验证玩家是否真正学会了“读懂风、调好帆、走对路”。

#### 2.4.3 成功 / 失败条件（缺失）

system graph 左栏定义：
- Win：Boat health > 0 && Game Time > 0
- Game Over：Boat Health < 0 || Game Time < 0

当前实现：
- 没有 `boat health`；
- 没有 `game time`；
- 没有 Game Over 面板；
- 到达目标后只显示“目标到达！”，然后 1.5s 后生成新目标。

**影响：** 玩家没有明确的胜负压力，也没有“第二次尝试会进步”的关卡结构。

#### 2.4.4 环境数据

system graph 列出：
- wind direction（Vector3）
- water current direction（Vector3）
- water current speed（float）
- rock positions（Vector3）
- target position（Vector3）
- game time（float）
- boat health（Int）
- other competitor（GameObject）

当前实现：
- wind direction ✅（但用标量角度，不是 Vector3）
- water current ❌ 完全缺失
- rock positions ❌ 完全缺失
- target position ✅（随机生成）
- game time ❌ 缺失
- boat health ❌ 缺失
- other competitor ❌ 缺失
- Exposed Data on screen 中的 Water Current Direction / Water Current Speed / Rock Positions 都没有

**影响：** 缺少水流和障碍物，核心学习挑战（“不能直航”）被削弱。

#### 2.4.5 反馈映射（部分实现）

brief 要求：
- Sail flaps → 帆角低效
- Wake shorter → 速度下降
- Boat drifts from target → 风/水流推偏路线
- Boat tilts strongly → 稳定性下降
- Wind indicator changes → 目标可能无法直航

当前实现：
- 有 HUD 显示速度、航向、风向、帆角、舵角、迎风角、调帆质量 ✅
- 有罗盘显示风来向、目标方位、禁航区 ✅
- 有“目标到达”提示 ✅
- 帆的颜色随 trim quality 变化 ✅（在 `Sailboat.js` 中体现）
- 尾迹系统存在（但 WakeSystem 主要渲染航迹泡沫，没有“wake 变短”作为速度反馈的显性表达）
- 没有“boat drifts from target”的航线偏离可视化（如规划航线 vs 实际航线对比）
- 没有 boat health / stability 的反馈（因为机制缺失）

**影响：** 反馈足够支持调帆，但不足以支撑“路径规划”和“避障”学习。

---

### 2.5 开发日志（完全缺失）

brief 要求维护 `agent-development-log.md`，包含：
- Raw Interaction Log（每次有意义交互后追加）
- Stage Reflection（里程碑时由学生填写）

当前项目：没有 `agent-development-log.md`。

**影响：** 无法满足提交要求；也无法展示“人机协作开发过程”。

---

### 2.6 提交与展览包装（完全缺失）

brief 要求：
- `submission-manifest.json`
- `preview.png`（16:9）
- 本地静态服务器测试
- 无网络依赖测试
- 相对路径检查
- 1920×1080 展览测试
- 移除 `.git`、`node_modules`、缓存等
- 打包成 `2026-Camp-Group-XX-ProjectName.zip`

当前项目：以上全部缺失。且由于依赖 Three.js + Vite，离线无网络运行需要额外处理（CDN 或本地副本）。

---

## 3. 核心学习循环验证

brief 的 Observe → Judge → Act → Feedback → Adjust 循环：

| 环节 | 当前实现 | 评价 |
|---|---|---|
| Observe | HUD + 罗盘 | 强 |
| Judge | “可直航 / 逆风·需之字形” 提示 | 弱：只有二值提示，没有“为什么”和“该怎么走” |
| Act | 转舵、调帆、缩帆 | 强 |
| Feedback | 速度、帆颜色、航行状态 | 中 |
| Adjust | 玩家自行调帆/转向 | 中，但缺少失败后的重试结构 |

**关键问题：** 没有关卡目标，就没有明确的“判断”标准；没有时间限制，也就没有“必须调整”的紧迫感。

---

## 4. 当前代码质量评估（与 brief 无关，仅作参考）

优点：
- 模块划分清晰（scene / physics / boat / controls / ui / utils）。
- 物理参数集中，便于调参。
- 主循环有异常保护，避免单帧错误卡死。
- 变量命名和注释对学生友好。

问题：
- 使用了超出 brief 允许的技术复杂度（Three.js 着色器海面、天空球等）。
- 部分功能与核心学习循环无关，分散了 first-playable 的焦点。
- 没有单元测试或玩法验证脚本。

---

## 5. 优先级建议（如果要转向系统化成果展示）

### P0 — 必须先做（否则无法提交）
1. **技术栈回退或兼容方案**：决定是保留 Three.js 还是按 brief 改为 Canvas 2D。若保留，必须将依赖改为 CDN 或本地副本，并确保无 npm 也能运行。
2. **重建项目结构**：创建 `index.html`、`game.html`、`process.html`、`styles.css`、`game.js`、`agent-development-log.md`、`submission-manifest.json`。
3. **实现关卡/挑战系统**：至少 2~3 个可切换的预设关卡（Level 1~3），包含固定起点、终点、风向、限时、岩石。
4. **实现成功/失败条件**：Win / Game Over 状态、计时器、boat health（可简化为碰撞扣血）。
5. **开始写 `agent-development-log.md`**：从项目初始化开始补录，后续每次有意义交互后追加。

### P1 — 显著提升展示质量
6. **搭建集成网站**：Game Idea、Domain Knowledge、System Design、Development Process、Play Game 五个部分。
7. **补充 system graph 图片**：将 graph 导出为 `system-graph.png` 并嵌入网站。
8. **生成 `preview.png`**：16:9 实际游戏截图。
9. **补充水流和岩石机制**：与 system graph 和 brief 一致。
10. **增强反馈**：帆拍打视觉/声音、尾迹长度随速度变化、航线偏离指示。

### P2 — 提交前收尾
11. 离线无网络测试。
12. 1920×1080 展览布局测试。
13. 清理 `node_modules`、缓存、个人路径。
14. 打包 ZIP 并填写 manifest。

---

## 6. 下一步需要你做的决策

在我继续动手改代码之前，请确认以下关键问题：

1. **技术栈**：是否同意按 brief 回退到“HTML Canvas 2D + 零外部库”？还是你坚持保留 Three.js 的 3D 效果？（后者需要做离线兼容处理。）
2. **关卡设计**：Level 1~3 的精确坐标、风向、限时、岩石布局你有初步想法吗？还是希望我基于 system graph 给出默认值？
3. **游戏页与网站页**：是否希望我直接生成 `game.html` 和 `process.html` 的模板？
4. **开发日志**：是否需要我帮你补写初始条目和已经发生的交互记录？

---

*报告生成时间：2026-07-12*
